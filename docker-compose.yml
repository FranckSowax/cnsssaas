# ============================================
# CNSS WhatsApp Platform - Deploiement On-Premise
# Usage: docker compose up -d
# ============================================

services:
  # ============================================
  # Application Node.js (API + Frontend)
  # ============================================
  app:
    build:
      context: .
      dockerfile: deploy/Dockerfile
    container_name: cnss-whatsapp-app
    env_file: .env
    environment:
      - NODE_ENV=production
      - PORT=3000
      - DATABASE_URL=postgresql://cnss_user:${DB_PASSWORD}@db:5432/cnss_whatsapp?schema=public
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - app_logs:/app/logs
    networks:
      - cnss-internal
    restart: unless-stopped

  # ============================================
  # PostgreSQL avec pgvector (BDD on-premise)
  # ============================================
  db:
    image: pgvector/pgvector:pg16
    container_name: cnss-whatsapp-db
    environment:
      POSTGRES_USER: cnss_user
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: cnss_whatsapp
    volumes:
      - pgdata:/var/lib/postgresql/data
    networks:
      - cnss-internal
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U cnss_user -d cnss_whatsapp"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ============================================
  # Nginx Reverse Proxy (SSL + IP filtering)
  # ============================================
  nginx:
    image: nginx:alpine
    container_name: cnss-whatsapp-nginx
    ports:
      - "443:443"
      - "80:80"
    volumes:
      - ./deploy/nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ./deploy/ssl:/etc/nginx/ssl:ro
    depends_on:
      - app
    networks:
      - cnss-internal
      - cnss-external
    restart: unless-stopped

volumes:
  pgdata:
    driver: local
  app_logs:
    driver: local

networks:
  cnss-internal:
    driver: bridge
    internal: true    # Pas d'acces Internet direct
  cnss-external:
    driver: bridge    # Acces Internet (webhooks + API)
